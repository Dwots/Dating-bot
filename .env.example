BOT_TOKEN=your_telegram_bot_token_here

# Optional. Use if Docker cannot reach api.telegram.org directly.
# Example for an HTTP proxy running on the host machine:
# TELEGRAM_PROXY=http://host.docker.internal:7890
TELEGRAM_PROXY=
